enum EasyShipSortTypes {
  record,
  baNumber,
  name,
  orderId,
  date,
  time,
  total,
  totalPV
}


enum InventorySortTypes {
  itemCode,
  itemName,
  pv,
  price,
  quantityOnHand,
  totalAccumulatedPrice,
  totalPV
}

enum StockTypes {
  onHand,
  outOfStock
}

enum OpenPoTypes {
  poNumber,
  date,
  time,
  totalPv,
  totalPrice,
  status,
  attachment,
}